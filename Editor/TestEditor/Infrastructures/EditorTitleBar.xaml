<?xml version="1.0" encoding="utf-8"?>
<UserControl
	x:Class="TestEditor.EditorTitleBar"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:local="using:TestEditor"
	xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
	xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	mc:Ignorable="d"
	DataContext="{Binding RelativeSource={RelativeSource Self}}">

	<Grid Name="titleContents"
		Background="{Binding Background, FallbackValue={ThemeResource AccentFillColorSelectedTextBackgroundBrush}}"
		HorizontalAlignment="Stretch" VerticalAlignment="Stretch"
		Margin="0,0,0,-1">

		<ContentControl>
			<ContentPresenter>
				
			</ContentPresenter>
		</ContentControl>

		<TextBlock Name="titleText"
			Text="{Binding Text, FallbackValue=Application}"
			Style="{Binding TextStyle, FallbackValue={ThemeResource BodyTextBlockStyle}}"
			Foreground="{Binding TextColor}"
			TextAlignment="{Binding TextAlignment, FallbackValue=Left}"
			HorizontalTextAlignment="{Binding HorizontalTextAlignment, FallbackValue=Left}"
			VerticalAlignment="Center"
			Margin="6,1,6,0"/>
	</Grid>
</UserControl>
