/// @description 서피스 제거
if surface_exists(popup_surface)
	surface_free(popup_surface)

if surface_exists(menu_blur_surface1)
	surface_free(menu_blur_surface1)

if surface_exists(menu_blur_surface2)
	surface_free(menu_blur_surface2)
